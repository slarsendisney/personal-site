import React from "react";
import Layout from "../components/layout";

const CV = () => {
  return (
    <Layout>
      <button className="btn-accent">Download CV</button>
    </Layout>
  );
};
export default CV;
